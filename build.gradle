plugins {
    id 'org.springframework.boot' version '2.3.4.RELEASE'
    id 'io.spring.dependency-management' version '1.0.10.RELEASE'
    id 'java'
}

group = 'org.projet_iwa'

allprojects {
    apply plugin: 'application'
    apply plugin: 'java'

    repositories {
        mavenCentral()
    }

    version = '2.3.4.RELEASE'
    dependencies{
        compile "org.springframework.boot:spring-boot-starter:" + version
        compile "org.springframework.boot:spring-boot-starter-data-jpa:" + version
    }

    test {
        useJUnitPlatform()
    }
}

project(":auth") {
    apply plugin: 'org.springframework.boot'

    dependencies {
//        compile project(":boot-logger")
//        compile project(":boot-exceptions")
    }
}

project(":alert") {
    apply plugin: 'org.springframework.boot'

    
}

project(":location") {
    apply plugin: 'org.springframework.boot'


}